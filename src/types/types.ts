export type CardType = {
  title: string
  slogan: string
  flavor: string
  portionSize: number | string
  portionSizeText: string
  miceSize: number | string
  miceSizeText: string
  portionWeight: string
  portionUnit: string
}
